setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    express: Dockerfile.express
    bootstrapping: Dockerfile.bootstrapping
  config:
    NODE_ENV: production
run:
  web: npm start

